# systemconfig

some system config script files

## run

run shell script run.sh by sudo

## description

git-config/
run git files after install git scm

apt-config/
run to update sources.list in apt

vim-config/
run to update vim config
